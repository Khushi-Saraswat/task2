# Application
spring.application.name=server
server.port=${PORT:8080}  # Railway sets this automatically

# MySQL Database (use Railway variables if present, else fallback to local)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3307/task}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:khu123_45}

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true
